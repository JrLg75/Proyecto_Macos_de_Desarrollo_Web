spring.application.name=techzone-peru

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/techzone_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.sql.init.mode=always

# Porque no deja subir al github
# openai.api.key=${OPENAI_API_KEY}

# Ruta donde se guardar�n las im�genes subidas
file.upload-dir=C:/Users/Joseph/Downloads/proyecto/techzone-uploads/

# Vertex AI
gcp.project-id=tech-474010
gcp.location=us-central1
vertex.model.name=gemini-2.5-pro

logging.level.org.springframework.security=DEBUG